import React from "react";
import BackgroundWithText from "./BackgroundWithText";

function SwiperHero({ destination }) {
  return <BackgroundWithText obj={destination} link={false} height={false} />;
}

export default SwiperHero;
